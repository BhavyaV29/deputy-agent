"""Phase 1 spike: does constrained decoding make a small local model emit
near-100% schema-valid tool calls?

This package is a self-contained experiment (client, tool schemas, prompt set,
validator, runner, report) and is intentionally decoupled from the eventual
agent so it can be deleted once the bet is settled.
"""
